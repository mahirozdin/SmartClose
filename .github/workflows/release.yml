name: Release

on:
  workflow_dispatch:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  release:
    runs-on: macos-latest
    env:
      APPLE_TEAM_ID: ${{ secrets.APPLE_TEAM_ID }}
      DEVELOPER_ID_APPLICATION_P12_BASE64: ${{ secrets.DEVELOPER_ID_APPLICATION_P12_BASE64 }}
      DEVELOPER_ID_APPLICATION_P12_PASSWORD: ${{ secrets.DEVELOPER_ID_APPLICATION_P12_PASSWORD }}
      NOTARYTOOL_APPLE_ID: ${{ secrets.NOTARYTOOL_APPLE_ID }}
      NOTARYTOOL_APP_PASSWORD: ${{ secrets.NOTARYTOOL_APP_PASSWORD }}
      SMARTCLOSE_TEAM_ID: WWRZ5CG3DW
      SMARTCLOSE_NOTARY_PROFILE: smartclose-ci
    steps:
      - uses: actions/checkout@v4

      - name: Check release secret availability
        id: release_secrets
        shell: bash
        run: |
          if [[ -n "${DEVELOPER_ID_APPLICATION_P12_BASE64:-}" && -n "${DEVELOPER_ID_APPLICATION_P12_PASSWORD:-}" ]]; then
            echo "ready=true" >> "${GITHUB_OUTPUT}"
          else
            echo "ready=false" >> "${GITHUB_OUTPUT}"
            echo "Developer ID p12 secrets are not configured. Skipping CI release signing."
          fi

      - name: Validate required secrets
        if: steps.release_secrets.outputs.ready == 'true'
        shell: bash
        run: |
          if [[ "${APPLE_TEAM_ID}" != "WWRZ5CG3DW" ]]; then
            echo "::error::APPLE_TEAM_ID must be WWRZ5CG3DW for SmartClose releases."
            exit 1
          fi

          missing=0
          for key in \
            APPLE_TEAM_ID \
            DEVELOPER_ID_APPLICATION_P12_BASE64 \
            DEVELOPER_ID_APPLICATION_P12_PASSWORD \
            NOTARYTOOL_APPLE_ID \
            NOTARYTOOL_APP_PASSWORD; do
            if [[ -z "${!key:-}" ]]; then
              echo "::error::Missing required secret: ${key}"
              missing=1
            fi
          done
          exit "${missing}"

      - name: Import Developer ID certificate
        if: steps.release_secrets.outputs.ready == 'true'
        shell: bash
        run: |
          CERT_PATH="${RUNNER_TEMP}/developer-id-application.p12"
          KEYCHAIN_PATH="${RUNNER_TEMP}/smartclose-release.keychain-db"
          KEYCHAIN_PASSWORD="$(openssl rand -base64 24)"

          echo "${DEVELOPER_ID_APPLICATION_P12_BASE64}" | base64 --decode > "${CERT_PATH}"

          security create-keychain -p "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
          security set-keychain-settings -lut 21600 "${KEYCHAIN_PATH}"
          security unlock-keychain -p "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
          security import "${CERT_PATH}" \
            -P "${DEVELOPER_ID_APPLICATION_P12_PASSWORD}" \
            -A \
            -t cert \
            -f pkcs12 \
            -k "${KEYCHAIN_PATH}"
          security list-keychains -d user -s "${KEYCHAIN_PATH}" login.keychain-db
          security default-keychain -d user -s "${KEYCHAIN_PATH}"
          security set-key-partition-list -S apple-tool:,apple: -s -k "${KEYCHAIN_PASSWORD}" "${KEYCHAIN_PATH}"
          IDENTITIES="$(security find-identity -v -p codesigning "${KEYCHAIN_PATH}")"
          echo "${IDENTITIES}"

          if ! grep -q "Developer ID Application: .*(${APPLE_TEAM_ID})" <<< "${IDENTITIES}"; then
            echo "::error::Imported certificate does not contain the SmartClose Developer ID identity for team ${APPLE_TEAM_ID}."
            exit 1
          fi

      - name: Configure notarytool profile
        if: steps.release_secrets.outputs.ready == 'true'
        run: |
          xcrun notarytool store-credentials "${SMARTCLOSE_NOTARY_PROFILE}" \
            --apple-id "${NOTARYTOOL_APPLE_ID}" \
            --team-id "${APPLE_TEAM_ID}" \
            --password "${NOTARYTOOL_APP_PASSWORD}"

      - name: Build, notarize, and package
        if: steps.release_secrets.outputs.ready == 'true'
        run: |
          chmod +x scripts/*.sh
          ./scripts/release_local.sh

      - name: Upload artifacts
        if: steps.release_secrets.outputs.ready == 'true'
        uses: actions/upload-artifact@v4
        with:
          name: SmartClose-release
          path: dist/

      - name: Publish GitHub Release
        if: steps.release_secrets.outputs.ready == 'true' && startsWith(github.ref, 'refs/tags/v')
        shell: bash
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          mapfile -t assets < <(find dist -type f -maxdepth 2 | sort)
          gh release create "${GITHUB_REF_NAME}" "${assets[@]}" \
            --title "SmartClose ${GITHUB_REF_NAME}" \
            --generate-notes
